<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homwFragment">

    <fragment
        android:id="@+id/homwFragment"
        android:name="com.example.notes.HomeFragment"
        android:label="fragment_homw"
        tools:layout="@layout/fragment_homw" >
        <action
            android:id="@+id/action_homwFragment_to_writeNoteFragment"
            app:destination="@id/writeNoteFragment" />
    </fragment>
    <fragment
        android:id="@+id/writeNoteFragment"
        android:name="com.example.notes.WriteNoteFragment"
        android:label="fragment_write_note"
        tools:layout="@layout/fragment_write_note" >
        <action
            android:id="@+id/action_writeNoteFragment_to_homwFragment"
            app:destination="@id/homwFragment" />
    </fragment>
</navigation>